os: osx
language: cpp

compiler:
    - clang


install: ./install.sh

script:
  - conan config install https://github.com/includeos/conan_config.git
  - conan create binutils/2.31 binutils/2.31@includeos/toolchain -pr apple-clang-10-macos-toolchain
  - conan search binutils/2.31@includeos/toolchain -r includeos
  - echo "UPLOADING ...."

notifications:
  slack: includeos:qyy8ZShmREcJsl6vD0Ae0Qsa
